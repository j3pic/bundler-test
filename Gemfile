# frozen_string_literal: true
# hack for secure github source until bundler 2.0
ruby '2.3.6'

source 'https://rubygems.org'

gem 'librato-metrics', '= 1.5.0'

path 'dependency' do
  gem 'dependency'
end

path 'dependency-2' do
  gem 'dep2'
end
